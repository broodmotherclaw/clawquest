name: Deploy Backend to Vercel

on:
  push:
    branches: [ main ]
    paths:
      - 'backend/**'
  workflow_dispatch:

env:
  NODE_VERSION: '18'
  VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
  VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
      
      - name: Cache dependencies
        uses: actions/cache@v3
        with:
          path: backend/node_modules
          key: ${{ runner.os }}-node-${{ env.NODE_VERSION }}-${{ hashFiles('backend/package-lock.json') }}
      
      - name: Install dependencies
        working-directory: ./backend
        run: npm ci
      
      - name: Install Vercel CLI
        run: npm install --global vercel@latest
      
      - name: Pull Vercel Environment
        working-directory: ./backend
        run: |
          echo "🔧 Pulling Vercel Environment..."
          rm -rf .vercel
          test -n "$VERCEL_ORG_ID"
          test -n "$VERCEL_PROJECT_ID"
          mkdir -p .vercel
          cat > .vercel/project.json <<JSON
          {"orgId":"$VERCEL_ORG_ID","projectId":"$VERCEL_PROJECT_ID"}
          JSON
          vercel pull --token=${{ secrets.VERCEL_OWNCLAW }} --scope "$VERCEL_ORG_ID" --yes --environment=production
          
      - name: Deploy to Vercel
        id: deploy
        working-directory: ./backend
        env:
          VERCEL_GIT_COMMIT_AUTHOR_LOGIN: ${{ github.actor }}
          VERCEL_GIT_COMMIT_AUTHOR_NAME: ${{ github.actor }}
          VERCEL_GIT_COMMIT_AUTHOR_EMAIL: ${{ github.actor }}@users.noreply.github.com
        run: |
          echo "🚀 Deploying to Vercel..."
          vercel --prod --scope "$VERCEL_ORG_ID" --token=${{ secrets.VERCEL_OWNCLAW }} --yes
      
      - name: Get deployment URL
        id: url
        working-directory: ./backend
        run: |
          DEPLOYMENT_URL=$(vercel ls --scope "$VERCEL_ORG_ID" --token=${{ secrets.VERCEL_OWNCLAW }} --prod --yes 2>/dev/null | grep -oP '(https://[^ ]+\.vercel\.app[^ ]*)' | head -1)
          test -n "$DEPLOYMENT_URL"
          echo "deployment-url=$DEPLOYMENT_URL" >> $GITHUB_OUTPUT
      
      - name: Update environment variables
        working-directory: ./backend
        run: |
          echo "🔧 Updating environment variables..."

          test -n "${{ secrets.DATABASE_URL }}"
          test -n "${{ secrets.SUPABASE_URL }}"
          test -n "${{ secrets.SUPABASE_ANON_KEY }}"
          test -n "${{ secrets.OPENCLAW_BOT_SECRET }}"

          sync_vercel_env() {
            local name="$1"
            local value="$2"
            vercel env rm "$name" production --scope "$VERCEL_ORG_ID" --token=${{ secrets.VERCEL_OWNCLAW }} --yes >/dev/null 2>&1 || true
            printf '%s' "$value" | vercel env add "$name" production --scope "$VERCEL_ORG_ID" --token=${{ secrets.VERCEL_OWNCLAW }}
          }

          sync_vercel_env "DATABASE_URL" "${{ secrets.DATABASE_URL }}"
          sync_vercel_env "SUPABASE_URL" "${{ secrets.SUPABASE_URL }}"
          sync_vercel_env "SUPABASE_ANON_KEY" "${{ secrets.SUPABASE_ANON_KEY }}"
          sync_vercel_env "OPENCLAW_BOT_SECRET" "${{ secrets.OPENCLAW_BOT_SECRET }}"
          sync_vercel_env "SHARED_SECRET" "${{ secrets.OPENCLAW_BOT_SECRET }}"
      
      - name: Wait for deployment
        working-directory: ./backend
        run: |
          echo "⏳ Waiting for deployment to be ready..."
          sleep 30
      
      - name: Test deployment
        working-directory: ./backend
        run: |
          DEPLOYMENT_URL="${{ steps.url.outputs.deployment-url }}"
          echo "🧪 Testing deployment at $DEPLOYMENT_URL..."

          BYPASS_SECRET="${{ secrets.VERCEL_AUTOMATION_BYPASS_SECRET }}"
          CURL_ARGS=(-s -L --max-redirs 10 -o /dev/null -w "%{http_code}")
          if [ -n "$BYPASS_SECRET" ]; then
            echo "🔐 Using Vercel protection bypass header for health check"
            CURL_ARGS+=(-H "x-vercel-protection-bypass: $BYPASS_SECRET" -H "x-vercel-set-bypass-cookie: true")
          fi

          HTTP_STATUS=""
          for attempt in $(seq 1 12); do
            HTTP_STATUS=$(curl "${CURL_ARGS[@]}" "$DEPLOYMENT_URL/api/health")
            if [ "$HTTP_STATUS" = "200" ]; then
              echo "✅ Health check passed (HTTP $HTTP_STATUS)"
              break
            fi
            echo "⌛ Attempt $attempt/12 returned HTTP $HTTP_STATUS, retrying..."
            sleep 10
          done

          if [ "$HTTP_STATUS" != "200" ]; then
            echo "❌ Health check failed (HTTP $HTTP_STATUS)"
            if [ "$HTTP_STATUS" = "401" ]; then
              echo "⚠️  401 indicates Vercel Deployment Protection. Set secret VERCEL_AUTOMATION_BYPASS_SECRET or disable protection for production."
            fi
            exit 1
          fi
      
      - name: Deployment success
        working-directory: ./backend
        run: |
          DEPLOYMENT_URL="${{ steps.url.outputs.deployment-url }}"
          echo "✅ Backend deployed successfully!"
          echo "🌐 URL: $DEPLOYMENT_URL"
          echo "📊 Check deployment status at Vercel dashboard"
